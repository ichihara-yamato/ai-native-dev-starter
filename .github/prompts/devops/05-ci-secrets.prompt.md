# DevOps: CI Secrets & Credentials

Role: DevOps
Purpose: Guidelines for managing secrets in CI workflows and secure access to environments.
Inputs: Secret types (API keys, DB creds), CI provider features, rotation policy.
Constraints: Never store secrets in repo; use secrets manager and least-privilege scopes.
Outputs: Examples for GitHub Actions secrets usage, vault integration snippets, and rotation checklist.
