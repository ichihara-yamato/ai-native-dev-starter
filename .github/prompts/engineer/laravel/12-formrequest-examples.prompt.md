# Engineer: FormRequest Examples

Role: Engineer
Purpose: Provide concrete `FormRequest` examples for common CRUD operations.
Inputs: Field definitions and edge-cases (file uploads, arrays, nested objects).
Constraints: Include `authorize()` examples and localized messages.
Outputs: Copy-pasteable `FormRequest` stubs and explanation of patterns.
