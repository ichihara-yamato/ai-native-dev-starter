# Engineer: Validation Rule Examples

Role: Engineer
Purpose: Provide common validation rule examples and patterns for FormRequests.
Inputs: Field definitions and constraints (unique, exists, date formats).
Constraints: Use rule arrays, prefer `Rule::unique()` for complex cases.
Outputs: Snippets to paste into `rules()` and `messages()`.
